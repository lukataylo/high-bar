# Secrets — never commit
.env
.env.*
!.env.example

# Dependencies
node_modules/
.pnpm-store/

# Build output
dist/
build/
.next/
.turbo/
out/
coverage/
*.tsbuildinfo

# Local runtime data (autonomous-loop generated; reproducible via `node scripts/autonomous-loop.mjs --seed`)
data/

# Loop-service deploy config — used by `railway up` for the agent-loop service only.
# Kept untracked so the GitHub-sourced web service never picks it up.
railway.json

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
