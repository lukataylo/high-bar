{
  "name": "high-bar-workspace",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.25.0",
  "scripts": {
    "dev": "pnpm --filter @high-bar/web dev",
    "build": "pnpm --filter @high-bar/web build",
    "start": "pnpm --filter @high-bar/web start",
    "lint": "pnpm --filter @high-bar/web lint",
    "typecheck": "pnpm --filter @high-bar/web typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.2"
  }
}
